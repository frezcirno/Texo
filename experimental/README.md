# Experiments

These files are unfinished exercises and are not part of `make all` or `make check`.

- `top_k.cu`: placeholder only; `solve` has no implementation and produces no output.

Move an exercise into `src/` only after adding a working build target, a CPU-reference
test, and a documented input/output contract.
